# SECURITY_CHECKLIST.md

> **维护说明**:
>
> - 填写者：`tech-leader` + `dev-engineer` (架构评审阶段 + 开发完成阶段)
> - 审核者：`qa-engineer` (安全测试前必须验证)
> - 用途：确保安全基线强制检查（防止硬编码、SQL 注入、CORS 错误等）

---

## 🔒 安全基线检查 (MANDATORY)

### 阶段 1: 架构评审 (tech-leader 负责)

**时间**: `architect` 交付架构后，`dev-engineer` 开始开发前

| 检查项     | 要求                                      | 状态      | 备注 |
| ---------- | ----------------------------------------- | --------- | ---- |
| 密钥管理   | 所有密钥必须从环境变量读取                | ⬜ 未开始 |      |
| 数据库设计 | 敏感字段加密要求明确                      | ⬜ 未开始 |      |
| API 安全   | 鉴权策略、权限模型定义清晰                | ⬜ 未开始 |      |
| CORS 策略  | 明确允许的域名列表，禁止 \* + credentials | ⬜ 未开始 |      |
| SQL 规范   | 强制参数化查询，禁止字符串拼接            | ⬜ 未开始 |      |
| 日志脱敏   | 密码/密钥/Token 禁止明文日志              | ⬜ 未开始 |      |

**架构评审结论**:

- [ ] **Pass** - 安全设计完整，允许开始开发
- [ ] **Conditional Pass** - 需修复以下问题后开始开发：
- [ ] **Fail** - 安全设计缺失，打回重新设计

**tech-leader 签字**: ******\_\_\_****** **日期**: ******\_\_\_******

---

### 阶段 2: 开发完成 (dev-engineer 负责)

**时间**: 开发完成后，交付 QA 前

#### 2.1 配置安全

| 检查项            | 检查方法                            | 预期结果                        | 实际结果 | 状态 |
| ----------------- | ----------------------------------- | ------------------------------- | -------- | ---- |
| 无硬编码密钥      | 搜索代码：`grep -r "your-secret" .` | 0 结果                          |          | ⬜   |
| 无硬编码密码      | 搜索代码：`grep -r "password.*=" .` | 仅配置示例                      |          | ⬜   |
| 环境变量读取      | 检查 `config.py` 使用 `os.getenv()` | 所有敏感配置                    |          | ⬜   |
| .env.example 完整 | 检查文件包含所有必填项              | 包含 JWT_SECRET, DB_PASSWORD 等 |          | ⬜   |
| 鉴权默认启用      | 检查 `AUTH_ENABLED` 默认值为 `True` | 默认开启                        |          | ⬜   |

#### 2.2 SQL 安全

| 检查项           | 检查方法                                    | 预期结果     | 实际结果 | 状态 |
| ---------------- | ------------------------------------------- | ------------ | -------- | ---- |
| 无字符串拼接 SQL | 搜索：`grep -r "f\"SELECT" .`               | 0 结果       |          | ⬜   |
| 参数化查询       | 检查所有 `cursor.execute()` 使用 `?` 或`%s` | 100% 参数化  |          | ⬜   |
| 表名白名单验证   | 检查动态表名是否有白名单校验                | 实现白名单   |          | ⬜   |
| 输入验证         | 检查用户输入是否有类型/长度验证             | 所有输入验证 |          | ⬜   |

#### 2.3 CORS 与中间件

| 检查项        | 检查方法                                                     | 预期结果       | 实际结果 | 状态 |
| ------------- | ------------------------------------------------------------ | -------------- | -------- | ---- |
| CORS 配置正确 | 检查 `allow_origins` 不是 `["*"]` 或配合 `credentials=False` | 符合安全要求   |          | ⬜   |
| 中间件无重复  | 检查 `main.py` 中间件只添加一次                              | 无重复         |          | ⬜   |
| 错误处理统一  | 检查异常返回使用 `json.dumps()`                              | 统一 JSON 格式 |          | ⬜   |

#### 2.4 异步与性能

| 检查项            | 检查方法                        | 预期结果       | 实际结果 | 状态 |
| ----------------- | ------------------------------- | -------------- | -------- | ---- |
| 异步接口用异步 DB | 检查 `async def` 内使用异步 ORM | 无同步阻塞调用 |          | ⬜   |
| 数据库连接池      | 检查连接池配置                  | 合理配置       |          | ⬜   |
| 超时设置          | 检查 DB/Redis 调用有 timeout    | 配置 timeout   |          | ⬜   |

#### 2.5 鉴权与授权

| 检查项         | 检查方法                      | 预期结果            | 实际结果 | 状态 |
| -------------- | ----------------------------- | ------------------- | -------- | ---- |
| 无测试后门     | 搜索：`grep -r "test_user" .` | 0 结果 (除测试文件) |          | ⬜   |
| 鉴权中间件生效 | 检查所有 API 路由有鉴权装饰器 | 100% 覆盖           |          | ⬜   |
| 权限验证       | 检查越权访问场景有验证        | 实现权限检查        |          | ⬜   |

#### 2.6 前端安全

| 检查项       | 检查方法                                | 预期结果     | 实际结果 | 状态 |
| ------------ | --------------------------------------- | ------------ | -------- | ---- |
| 外部脚本 SRI | 检查 `<script>` 标签有 `integrity` 属性 | 所有外部脚本 |          | ⬜   |
| CSP 策略     | 检查响应头有 `Content-Security-Policy`  | 配置合理     |          | ⬜   |
| 静态目录限制 | 检查静态文件挂载不暴露源码              | 仅公开资源   |          | ⬜   |

#### 2.7 代码质量

| 检查项          | 检查方法                                  | 预期结果          | 实际结果 | 状态 |
| --------------- | ----------------------------------------- | ----------------- | -------- | ---- |
| 无 TODO/FIXME   | 搜索：`grep -r "TODO\|FIXME" .`           | 0 结果 (或已记录) |          | ⬜   |
| 无占位数据      | 搜索：`grep -r "example\|sample\|mock" .` | 仅测试文件        |          | ⬜   |
| 构建通过        | 执行 `npm run build`                      | 0 错误            |          | ⬜   |
| TypeScript 严格 | 检查 `tsconfig.json` 无隐式 any           | 严格模式          |          | ⬜   |

---

### 阶段 3: QA 验证 (qa-engineer 负责)

**时间**: E2E 测试前

| 检查项       | 测试方法                | 预期结果    | 实际结果 | 状态 |
| ------------ | ----------------------- | ----------- | -------- | ---- |
| SQL 注入测试 | 尝试注入表名/特殊字符   | 被拦截/转义 |          | ⬜   |
| 越权访问测试 | 尝试访问他人数据        | 返回 403    |          | ⬜   |
| 鉴权绕过测试 | 尝试无 Token 访问       | 返回 401    |          | ⬜   |
| CORS 测试    | 跨域带凭据请求          | 符合预期    |          | ⬜   |
| 敏感信息泄露 | 检查响应/日志无明文密码 | 无泄露      |          | ⬜   |

---

## 📊 检查结果汇总

### 阶段 1: 架构评审

- **状态**: ⬜ 未开始 / 🟡 进行中 / ✅ Pass / ❌ Fail
- **问题数**: P0: ** P1: ** P2: \_\_
- **tech-leader 签字**: ******\_\_\_****** 日期：******\_\_\_******

### 阶段 2: 开发完成

- **状态**: ⬜ 未开始 / 🟡 进行中 / ✅ Pass / ❌ Fail
- **检查项总数**: **\_**
- **通过项数**: **\_**
- **失败项数**: **\_**
- **dev-engineer 签字**: ******\_\_\_****** 日期：******\_\_\_******

### 阶段 3: QA 验证

- **状态**: ⬜ 未开始 / 🟡 进行中 / ✅ Pass / ❌ Fail
- **安全测试覆盖率**: **\_**%
- **发现漏洞数**: P0: ** P1: ** P2: \_\_
- **qa-engineer 签字**: ******\_\_\_****** 日期：******\_\_\_******

---

## 🚨 违规记录

| 日期 | 违规类型 | 发现者 | 严重性 | 修复状态 |
| ---- | -------- | ------ | ------ | -------- |
| -    | -        | -      | -      | -        |

---

## 📝 使用说明

1. **阶段 1** 必须在开发开始前完成，`tech-leader` 不签字 → `dev-engineer` 不准开始
2. **阶段 2** 必须在交付 QA 前完成，有任何 ❌ → `qa-engineer` 拒绝接收
3. **阶段 3** 必须在发布前完成，有任何 P0 漏洞 → `devops-engineer` 不准发布
4. 所有检查记录必须填写到 `VALIDATION_LOG.md` 附件中

---

**模板版本**: 1.0 **最后更新**: 2026-03-20
